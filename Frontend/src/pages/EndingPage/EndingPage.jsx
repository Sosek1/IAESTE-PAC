import classes from "./EndingPage.module.css";
import { colors } from "@mui/material";

const EndingPage = () => {
  return(
    <main className={classes.EndingPage}>
      <header>
        <h1>Do zobaczenia na rekrutacji!</h1>
      </header>
    </main> 
  );
};

export default EndingPage;
